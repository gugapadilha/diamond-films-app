<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.telalogin">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".FilmsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_diamond"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_diamond_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TelaLogin"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.AccountActivity" />
        <activity android:name=".ui.films.FilmsActivity" />
        <activity android:name=".ui.PasswordActivity" />

    </application>

</manifest>